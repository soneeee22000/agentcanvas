# AgentCanvas runs a deterministic MOCK agent out of the box — no key, no cost.
# Provide an Anthropic key to swap the mock for a real streaming agent loop.
ANTHROPIC_API_KEY=
AGENTCANVAS_MODEL=claude-sonnet-4-6
PORT=8787
